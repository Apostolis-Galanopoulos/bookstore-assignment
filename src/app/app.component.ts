import { Component } from '@angular/core';

@Component({
  selector: 'bookstore-root',
  template: '<router-outlet></router-outlet>'
})
export class AppComponent {
  title = 'bookstore-assignment';
}
